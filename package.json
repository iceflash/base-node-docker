{
  "name": "base-node-docker",
  "version": "1.0.0",
  "description": "base nodejs project in docker with postgres",
  "main": "index.js",
  "scripts": {
    "docker:dev": "docker-compose -f docker-compose.dev.yml up --detach project",
    "docker:dev:build": "docker-compose -f docker-compose.dev.yml up --build --detach project",
    "docker:down": "docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "base",
    "nodejs",
    "docker"
  ],
  "author": "Iakovle Denis (iceflash)",
  "license": "ISC"
}
